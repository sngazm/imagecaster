# 切り抜き動画 仕様

管理画面で、AI が選んだ切り抜きを**動画にする前に**音声と字幕で確かめ、その場で直し、
OK を出す。OK を出したものだけが縦・横・正方形の 3 本に描かれ、予定日に投稿される。

生成側の道具は別リポジトリ（[imagecaster-video](https://github.com/sngazm/imagecaster-video)）。
そちらの `SPEC.md` に、なぜその形なのかが書いてある。

## 何ができればよいか

1. 動画を描く前に、音声と字幕だけで仕上がりを確かめられる
2. 字幕の文字・改行・出す／出さないを、その場で直せる
3. 繋いだ区間の端を、その場で動かせる。区間を外す・戻す、継ぎ目の間を変える
4. 縦・横・正方形のどれで描いても、プレビューで見た字幕とずれない
5. OK を出すと 3 本が描かれ、予定日に X・Bluesky・YouTube Shorts・Instagram へ出る

スマホから触れることを前提に組む。

## 誰が何をするか

**描画はクラウドではできない。** ffmpeg も素材も手元（Mac / WSL）にある。一方、
確かめて直すのに描画は要らない。音声は R2 にあり、字幕は文字と時刻の並びでしかない。
だから**確かめて直すところまでを管理画面で済ませ、手元には決まったものを描かせるだけ**にする。

```
手元（Mac / WSL）                 管理画面（Cloudflare）
  AI が区間を選んで並べ、下書きを作る
  （区間の並び・字幕の区切り・文字時刻・画像）
        ↓ R2 に置く  ──→   繋いだ音声＋字幕でプレビュー
                             文字・改行・区間の端を直す
                             OK ＋ 投稿日時
        ↓ watch.py が見つける ←──
  下書きのとおりに 3 本描く
        ↓ R2 に上げる  ──→   Cron が予定日に投稿する
```

R2 を共有の置き場にするのは、既存の音声・書き起こしと同じ考え方。手元から見て
公開 URL で読め、Worker から書ける。新しい経路を増やさない。

手元でしか作れないのは、どこをどの順に繋ぐかの判断（AI）、字幕の区切り（LLM）、文字ごとの
時刻（`align.py`）、無音の検出。管理画面はこれを作らない。渡されたものを鳴らし、端を動かし、
文字を書き換えるだけ。**手元は音声を作らない。** 繋ぐ部分を示すデータだけを置く。

## R2 の置き場

```
episodes/{storageKey}/clips/
├── index.json                  この回の切り抜き一覧
└── {clipId}/
    ├── meta.json               状態・版の一覧・投稿の予定と結果
    ├── draft.json              下書き。プレビューと編集の対象
    ├── v1/
    │   ├── portrait.mp4        1080x1920
    │   ├── landscape.mp4       1920x1080
    │   ├── square.mp4          1080x1080
    │   ├── draft.json          描いたときの下書きの控え
    │   └── manifest.json       画像の出どころ
    └── v2/ …
```

版は上書きしない。OK を取り消して直し、描き直すと v2 になる。投稿済みの動画が
どの下書きから描かれたかを後から追えるようにするため。

### draft.json

切り抜きは、連続した 1 区間とは限らない。離れた区間を選び、並べ替え、間を落として
1 本に繋ぐ（カット編集）。**どこをどの順に繋ぐかを決めるのは手元の AI の仕事**で、下書きは
その結果を「区間の並び」として持つ。管理画面は並びを鳴らして確かめ、端を微調整する。

```json
{
  "revision": 4,
  "speed": 1.2,
  "gap": 0.12,
  "edgeFade": 0.03,
  "lead": 0.18,
  "trail": 0.22,
  "audio": { "format": "mp3-cbr", "headerBytes": 160, "bitrate": 192000, "sampleRate": 44100, "skipSamples": 0 },
  "pool": [{ "start": 480.0, "end": 510.0 }, { "start": 555.0, "end": 580.0 }],
  "spans": [
    { "id": "p3", "group": "g2", "start": 562.08, "end": 570.4, "gap": 0.3, "note": "落ち" },
    { "id": "p1", "group": "g1", "start": 487.74, "end": 491.2, "gap": 0.12 },
    { "id": "p2", "group": "g1", "start": 491.9, "end": 494.9, "gap": null, "off": false }
  ],
  "subs": [
    {
      "id": "s12",
      "speaker": "あずま",
      "start": 487.92,
      "end": 490.1,
      "of": "切層もない",
      "chars": [487.92, 488.1, 488.31, 488.5, 488.7],
      "rows": ["節操もない"],
      "skip": false
    }
  ],
  "cards": [{ "at": 489.0, "word": "…", "image": "https://…" }]
}
```

- **時刻はすべてエピソードの音声上の秒。** 繋いだあとの時刻は持たない。区間の並びから
  そのつど計算する（下の「繋いだあとの時刻」）。端を動かすたびに全部の字幕を書き換えずに済む
- `spans` は**鳴らす順**に並ぶ。元の音声での順とは限らない。`start`〜`end` を切り出し、
  そのあとに `gap` 秒の間を挟む（`null` なら下書きの `gap`）。最後の区間の `gap` は末尾の余韻
- `lead` / `trail` は、区間の端を字幕に合わせて置くときに前後へ足す余白。字幕の `start` は最初の字が
  鳴り始めた時刻なので、そこちょうどで切ると頭の子音が食われる
- `group` は AI が選んだひとまとまり。無音を詰める（tighten）と 1 つのまとまりが複数の
  区間に割れるので、画面ではまとまりごとに束ねて見せる。`note` は AI がそこを選んだ理由
- `off: true` の区間は鳴らさない。消さずに外しておけば戻せる
- `pool` は下書きが字幕を持っている範囲。AI が選んだ区間の前後に**余白 15 秒**を付けて作る。
  区間の端は `pool` の中でしか動かせない。それより外へ広げたい・別の場所を足したい・
  並べ替えたいときは、手元で下書きから作り直す
- `subs` は `pool` 全体について、元の音声の順に並ぶ。**画面に出るのは、読み上げの時刻
  （`chars`）が全部どれかの区間に入っている枚だけ。** 一部だけ入っている枚があれば、それは
  区間の端が字幕の途中にあるということで、OK を出せない
- `of` は元の書き起こしの文字、`chars` はその 1 字ごとの読み始め。**管理画面は
  この 2 つを書き換えない。** 直すのは `rows`（画面に出す文字と改行）と `skip` だけ
- `rows` を連ねたものが `of` と違えば、それは文字を直したということ。読み上げの
  進み（白くなっていく位置）は、`of` の進みを `rows` の字数に比例で写す
- `skip: true` は字幕を出さない。音は残る
- 枚を割る・繋ぐ：割るときは `of` と `chars` を同じ位置で切る。繋ぐときは連ねる。
  どちらも `of` を連ねた全体は変わらない。区間の端を字幕の途中に置きたいときは、先に割る
- 元の書き起こしに無い字幕を足すときは `of: ""`、`chars: []` とし、`start` / `end` を
  前後の隙間に置く
- `speed` は再生の速さ。**既定は 1.2**（ショート動画は少し速いほうが見やすい）。管理画面で
  変えられる。繋いだ音声と映像を、最後に丸ごと速める。字幕や画像の出入りも同じだけ速くなり、
  プレビューと動画で同じ速さになる。画面に出す尺は速めたあとの秒（投稿先の上限に効くのはそちら）
- `revision` は保存のたびに 1 増える。保存は「いま見ている revision」を添えて送り、
  食い違えば弾く（スマホと PC で同時に開いたときに、黙って上書きしない）

#### 繋いだあとの時刻

鳴らす区間（`off` でないもの）を順に並べ、i 番目の頭を τᵢ とすると

```
τ₀ = 0
τᵢ₊₁ = τᵢ + (endᵢ − startᵢ) + gapᵢ
元の時刻 t（startᵢ ≤ t < endᵢ）→ τᵢ + (t − startᵢ)
```

区間は頭を含み、尻を含まない。

字幕の出入り・読み上げの進み・画像の出入り・冒頭と末尾の暗転は、すべてこの τ の上で決める。
生成側（`splice.py`）と管理画面（`clipTimeline.ts`）が同じ式を使う。

#### 音

**手元は音声を作らない。** 管理画面が、エピソードの mp3 から `pool` の範囲だけを HTTP Range で
取り、ブラウザで復号し、本番と同じ規則（区間ごとに `edgeFade` 秒のフェード → `gap` 秒の無音）で
繋いだ波形を作って鳴らす。端を動かしたら、その場で繋ぎ直す。

- mp3 が固定ビットレートなら、時刻とバイト位置は比例する。途中のフレームだけを復号しても
  ffmpeg の復号と 1 サンプルもずれないことを実測で確かめてある（前に 12 フレーム余分に取り、
  ビットリザーバが落ち着くまでを捨てる）
- `audio` は手元がその回の mp3 を調べて書く。固定ビットレートでない回は `format` が変わり、
  管理画面はその回のプレビューを断る（そのときに別の道を作る）
- 繋いだ波形は WAV にして `<audio>` で鳴らす。速さは `playbackRate` で変える。ブラウザが音程を
  保ったまま速めるので、Web Audio で直接鳴らすより確かめやすい

### meta.json

```json
{
  "id": "c1",
  "episodeId": "285",
  "label": "脳が5つにちぎれる",
  "status": "approved",
  "approvedRevision": 4,
  "latest": 1,
  "versions": [
    { "n": 1, "createdAt": "…", "revision": 4, "layouts": ["portrait", "landscape", "square"] }
  ],
  "publishAt": "2026-09-27T12:00:00Z",
  "postText": "…",
  "posts": {
    "bluesky": { "enabled": true, "layout": "square", "postedAt": null, "url": null, "error": null },
    "x": { "enabled": true, "layout": "landscape", "postedAt": null, "url": null, "error": null },
    "youtube": { "enabled": true, "layout": "portrait", "postedAt": null, "url": null, "error": null },
    "instagram": { "enabled": true, "layout": "portrait", "postedAt": null, "url": null, "error": null }
  }
}
```

`status`:

| 値 | 意味 |
|---|---|
| `draft` | 下書き。管理画面で直せる |
| `approved` | OK が出た。描画待ち。下書きは直せない |
| `rendered` | `approvedRevision` の 3 本が揃った。投稿待ち |
| `published` | 投稿が済んだ（一部が失敗していても、出せるものは出した） |
| `rejected` | ボツ |

`approved` と `rendered` は OK を取り消せば `draft` に戻る。`published` は戻らない。

## 字幕の置き方

**字の位置を描画エンジンに決めさせない。** 行をまるごと渡して描かせると、ブラウザは
約物の連続を勝手に詰め、行の幅を整数に丸め、その挙動がブラウザごとに違う。Pillow とも
合わない。プレビューと動画で字幕を一致させるには、どちらも同じ表から同じ式で座標を出し、
1 字ずつそこへ置く。

- フォントは Noto Sans JP（可変）の wght 700。管理画面も同じファイルを読む
- 送り幅はフォントの表（1em = 1000）から引く。表は生成側が `clip metrics` で書き出し、
  管理画面に同梱する（`apps/admin/src/lib/clip-metrics.json`）。カーニングと約物詰めは掛けない
- 行は書記素（結合文字・異体字セレクタを含めた 1 字）に分け、送り幅を足して x を出す。
  行は中央に寄せる。ベースラインは行の上端から 1.16em
- 表に無い字（絵文字など）は 1em として置き、管理画面で警告する。環境によって
  別のフォントで描かれ、見た目が揃わない
- 読み終えた字は白、これからの字は薄く。薄いほうは**不透明な色で決める**。半透明の
  合成は Pillow と canvas で規則が違う

### 改行は 3 レイアウトで共通

1 行の上限を **em（字数）で決める**。レイアウトごとに変えるのは字の大きさと位置だけ。
プレビューで 1 回確かめれば、3 本とも同じ改行で出る。横型は幅が余っても短い行の
2 段になるが、確かめる手間が 3 倍になるよりよい。

| | 縦 | 横 | 正方形 |
|---|---|---|---|
| 大きさ | 1080x1920 | 1920x1080 | 1080x1080 |
| 1 行の上限 | 15em | 15em | 15em |
| 字の大きさ | 68px | 80px | 64px |

字の大きさ・字幕の高さ・人物や画像カードの位置は、生成側の `config.py` が持ち、
`clip-metrics.json` に一緒に書き出す。管理画面はそれを読んでガイド線を引く。数値を
二箇所に書かない。

### 揃っていることの確かめ方

```bash
# imagecaster-video で
uv run --with playwright python scripts/glyph-parity.py
```

管理画面の `clipGlyphs.ts` をそのまま Chromium で動かし、生成側の `Renderer` が描いた
ものと重ねて、1 字ごとの位置のずれを測る。約物の連続・欧文・半角カナ・異体字セレクタ・
結合文字・上限いっぱいの行などを 3 レイアウトで見る。`clip-metrics.json` が古くなって
いないかも見る。**字幕の描き方か `config.py` を変えたら、これを通すこと。**

残る差は縁のアンチエイリアスと、Pillow が字の x を整数に丸めること（±0.5px）だけ。

### ガイド線

プレビューは 3 レイアウトを切り替えて見られる。枠の中に次を線で出す。

- 字幕の 1 行の上限（これを越えた行は赤くなり、OK を出せない）
- 人物と画像カードが占める場所
- 投稿先の UI がかぶる場所（Shorts / Reels の右下と下端）

## API

既存の `/api/episodes/:id/...` に揃える。

| Method | Path | 何をするか |
|---|---|---|
| GET | `/api/episodes/:id/clips` | 一覧 |
| GET | `/api/episodes/:id/clips/:clipId` | meta.json |
| PUT | `/api/episodes/:id/clips/:clipId` | 生成側が下書きを置く |
| GET | `/api/episodes/:id/clips/:clipId/draft` | 下書き |
| PUT | `/api/episodes/:id/clips/:clipId/draft` | 下書きを保存する（revision を添える） |
| PUT | `/api/episodes/:id/clips/:clipId/status` | OK（投稿日時・本文・投稿先つき）／取り消し／ボツ |
| POST | `/api/episodes/:id/clips/:clipId/upload-url` | 動画を置く先を渡す（レイアウトごと） |
| POST | `/api/episodes/:id/clips/:clipId/versions` | 生成側が描いた版を登録する |
| GET | `/api/clips/pending` | 描画待ち（`approved`）の切り抜き。手元が拾う |

動画そのものは R2 の公開 URL から直接読む。音声と同じ扱いで、Worker を通さない。
**上げるほうも通さない。** 数十 MB を Worker に流す理由がないので、Presigned URL を
渡して手元から直に R2 へ入れさせる。

生成側は「動画を 3 本置く → 版として登録する」の順で呼ぶ。逆にすると、登録は済んで
いるのに動画が無い版が画面に出る。

下書きの保存では Worker が検算する。`of` を連ねたものが保存前と変わっていないこと、
`chars` の数が `of` の字数と合うこと、区間が `pool` の中にあって互いに重ならないこと、
`pool` と `audio` が変わっていないこと。合わなければ弾く。

`/api/clips/pending` と投稿の Cron は**全走査しない**。全エピソードの meta.json を読む
作りだと Worker のリソース制限（Error 1102）に達する。`index.json` に
`"エピソードID/切り抜きID"` の索引を 2 本持つ。

| 索引 | 入るとき | 出るとき |
|---|---|---|
| `clipRenderIds` | OK が出た | 版が登録された／OK が取り消された |
| `clipPostIds` | 版が登録された | 全部の投稿先が済んだ／OK が取り消された |

## 画面

`/episodes/:id/clips/:clipId`。スマホでは縦に積む。

```
┌─────────────────────────┐
│ ← #285 脳が5つにちぎれる         │
├─────────────────────────┤
│  [縦] [横] [正方形]             │   レイアウトの切り替え
│  ┌───────┐                │
│  │ ガイド線  │                │   canvas。字幕は動画と同じ座標で描く
│  │  字幕    │                │
│  └───────┘                │
├─────────────────────────┤
│ ▓▓▓▓│▓▓▓│▓▓▓▓▓▓│▓▓▓▓  │   繋いだあとの時間。区切り＝継ぎ目
├─────────────────────────┤
│ 0:02 え、ちなみに           [⋯] │   押すとその時刻へ。文字はその場で直せる
│ 0:04 基本的にコマンド。     [⋯] │   ⋯ ＝ 割る・前と繋ぐ・出さない・ここに足す
│ ⋮                             │
├─────────────────────────┤
│ 投稿 9/27 21:00  [本文] [投稿先] │
│                  [ボツ] [OK]    │
└─────────────────────────┘
```

- 音は、区間を繋いだ波形をブラウザで作って鳴らす（上の「音」）
- 字幕はまとまり（`group`）ごとに束ねて、鳴らす順に並べる。まとまりの頭に AI の `note` を出す
- 字幕の文字は枚の中で直す。改行がそのまま `rows` の区切りになる
- まとまりの端は、字幕 1 枚ぶんずつ伸び縮みさせる。枚の途中では切らない（切りたければ先に割る）。
  そのうえで端を 0.02 秒ずつ寄せられる。隣の字幕の読み上げには食い込めない
- まとまりを丸ごと外す・戻す。継ぎ目の間（`gap`）を変える
- 直したものは自動で保存する。OK を出せるのは、保存が済んでいて、上限を越えた行と
  表に無い字の警告が無いときだけ
- 描画が済んだら同じ画面で 3 本を再生できる

## 描画

1. 手元の `watch.py` が `/api/clips/pending` を見て拾う
2. `clip render <回> --clip <切り抜き>` が下書きを取り、区間の並びのとおりに繋いで、縦・横・正方形で
   描く。**区切りも文字も決め直さない。** LLM は呼ばない。判断が無いので Claude Code も開かない
3. 3 本を R2 に上げ、版として登録する。描いている間に OK が取り消されて直されていたら、
   登録は弾かれる

速さは、あとから掛けない。フレーム i に時刻 i × 速さ ÷ 30 を描かせ、それを 30fps として書き出す。
音だけを別に速めて合わせる。描き直しもコマ落ちも無い。

生成側は描く前に、下書きの文字（`of`）が、いまの書き起こしの同じあたりに同じ並びで載っているかを
確かめる。合わなければ止める。字幕と音がずれるのを防ぐための関門で、下書きを管理画面で直せるように
なっても緩めない。管理画面が `of` を書き換えないのはこのため。下書きを作ったあとで書き起こしが
取り直されると、ここで止まる。

**プレビューと動画が同じになることの確かめ方**（どちらも imagecaster-video で）：

```bash
uv run --with playwright python scripts/glyph-parity.py      # 字の位置
uv run python scripts/timeline-parity.py 下書き.json          # いつ何が出るか
```

後者は、管理画面の `clipTimeline.ts` をそのまま node で動かし、生成側の `timeline.py` と比べる。
1/60 秒刻みで「どの枚が出ていて、何字目まで白いか」まで見る。端を動かした・枚を外した・文字を
直した下書きも通すこと。

**文字を直して直るのはその動画の字幕だけ**で、公開サイトの文字起こしは誤字のまま。
誤認識は文字起こし側の失敗なので、目立つものは辞書に回す。ここでやるのは応急処置。

## 投稿

Cron（5 分おき）が `clipPostIds` を見て、`status` が `rendered` で `publishAt` を
過ぎたものを投稿する（`services/clip-posts.ts`）。投稿先ごとに結果を `meta.posts` に残し、
失敗した先だけ次の回でやり直す。1 つの失敗で他の投稿先を止めない。5 回失敗したら諦め、
人がやり直しを指示するまで試さない。

動画を上げてから向こうの処理が済むまで待つ投稿先は、1 回の Cron では終わらない。途中経過
（`posts[先].state`）を控えて、次の回が続きからやる。骨組みは投稿先を知らない。投稿先ごとの
手順は `Poster` に閉じ込め、`index.ts` で登録する。登録の無い投稿先には Worker は触らない。

| 投稿先 | 既定のレイアウト | 出し方 |
|---|---|---|
| Bluesky | 正方形 | Worker から。実装済み（`clip-post-bluesky.ts`） |
| Instagram（Reels） | 縦 | Worker から出せる見込み。未実装 |
| YouTube Shorts | 縦 | Worker から上げられるが、下の関門がある。未実装 |
| X | 横 | 未定（下を参照） |

以下は 2026-09-20 に公式の文書を読んで確かめたこと。実装する前に、原文をもう一度見ること。

### Bluesky

- アプリパスワードで動画を上げられる。アカウントのメール確認が済んでいること
- サービス認証（`com.atproto.server.getServiceAuth`）の `aud` は動画サービスではなく**自分の PDS**
  （`did:web:<PDS のホスト>`）、`lxm` は `com.atproto.repo.uploadBlob`、`exp` は**整数**で 1 時間以内
- `video.bsky.app` の `app.bsky.video.uploadVideo` に `Content-Length` つきで送る。Workers では
  R2 のストリームを `FixedLengthStream` に通す（そのまま渡すと chunked になる）
- 処理の状況（`getJobStatus`）は認証なしで聞ける。`already_exists` のエラーでも blob が返るので、
  成否より先に blob の有無を見る
- 上限 300MB。1 日 25 本 / 10GB（`getUploadLimits` で残りが分かる）。本文は 300 書記素。
  リンクは自動では付かないので facet を付ける。動画とリンクカードは同時に付けられない
- 出典: bluesky-social/bsky-docs `docs/tutorials/video.mdx`、atproto `lexicons/app/bsky/video/`

### Instagram（Reels）

- プロアカウント（ビジネス / クリエイター）が要る。**自分のアカウントにだけ出すなら、アプリ審査も
  ビジネス認証も要らない**（Instagram Login の系統、Standard Access）
- `POST /{ig-id}/media`（`media_type=REELS`、`video_url`）→ `status_code` が `FINISHED` になるのを待つ
  （公式の目安は 1 分おき・5 分まで）→ `POST /{ig-id}/media_publish`。コンテナは 24 時間で失効
- `video_url` は**公開 URL**。向こうが取りに来る。R2 の公開 URL で足りる
- 動画は moov atom が先頭（`+faststart`）、H.264、AAC 48kHz 以下、3 秒〜15 分、300MB まで
- トークンは 60 日。24 時間たてば更新でき、更新でまた 60 日。**放置して切れると人手で再発行**。
  Worker の secret は Worker から書き換えられないので、トークンは R2 に持つ
- 未確認: 開発モードのまま出した投稿が、アプリにロールを持たない人から見えるか。最初の 1 本を
  出して、ログアウトしたブラウザから確かめること
- 出典: developers.facebook.com `docs/instagram-platform/content-publishing`、`…/app-review`

### YouTube Shorts

- **2020-07-28 より後に作った API プロジェクトは、審査（compliance audit）を通るまで、API から上げた
  動画が非公開に固定される。** これから作るプロジェクトは必ず当たる。OAuth の同意画面の審査とは別物。
  申請は「YouTube API Services - Audit and Quota Extension Form」。個人でも出せるが、期間は書かれていない
- OAuth の同意画面を「Testing」のままにすると、リフレッシュトークンが 7 日で切れる。
  「In production」（個人利用・未検証）か、Workspace の「Internal」にする
- アップロードは resumable upload。`videos.insert` は専用の枠で 1 日 100 回（2026-06-01 から）
- Shorts かどうかは API では指定できない。3 分以内で、正方形か縦長なら YouTube が判定する
- 先に非公開で上げて `status.publishAt` で予約公開できる（処理が公開時刻までに終わる）。ただし上の
  固定が掛かっていると、公開されない可能性が高い
- 出典: developers.google.com `youtube/v3/docs/videos/insert`、`…/guides/quota_and_compliance_audits`

### X

- **API を使わない自動操作は、X の自動化ルールが名指しで禁じている。**
  「Use non-API-based forms of automation, such as scripting the X website. The use of these techniques
  may result in the permanent suspension of your account.」（X Automation Rules）
- API は従量課金だけになっている。ポスト 1 件 $0.015、URL を含むと $0.20（2026-04-16 改定）。
  動画のアップロード自体に課金の行は無い
- Web 版の予約投稿は、人が使うぶんには制限の記載が無い
- 動画は非 Premium で 140 秒 / 512MB。解像度の上限は 1920x1200 と 1200x1900 と書かれていて、
  縦 1080x1920 は文面上これを越える（実際に弾かれるかは未確認）
- 出典: help.x.com `rules-and-policies/x-automation`、docs.x.com `x-api/getting-started/pricing`

## 作る順番

1. ~~字幕の置き方を生成側と管理画面で揃え、ピクセルの差分で確かめる~~
2. ~~正方形のレイアウトと `clip-metrics.json` の書き出し~~
3. ~~下書きの型・API・テスト（Worker）~~
4. ~~プレビューと編集の画面~~（Chromium で確かめた。iOS の Safari では、mp3 の途中からの復号が
   1 サンプルも違わずに合うかをまだ確かめていない）
5. ~~手元の道具~~（R2 へ動画を上げるところだけは、鍵の要る本番でしか通せない）
   - ~~`clip draft`：区間・字幕の区切り・画像から下書きを作る。`--publish` で管理画面に置く~~
   - ~~`clip render`：下書きのとおりに 3 本描く。`--clip` で管理画面の下書きを取り、描いたら上げる~~
   - ~~繋いだあとの時刻・字幕の出入り・読み上げの進みが `clipTimeline.ts` と同じになることを、
     同じ下書きを両方に通して確かめる（`scripts/timeline-parity.py`）~~
   - ~~`watch.py`：描画待ち（`/api/clips/pending`）を拾って描く~~
   - **Worker を出すのと、Mac の `watch.py` を新しくするのは同じ日に**
6. 投稿（Bluesky → X → YouTube → Instagram）
