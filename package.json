{
  "name": "podcast-platform",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "dev:web": "turbo dev --filter=web",
    "dev:admin": "turbo dev --filter=admin",
    "dev:worker": "turbo dev --filter=worker",
    "test:worker": "turbo test --filter=worker",
    "deploy:worker": "turbo deploy --filter=worker",
    "deploy:web": "turbo deploy --filter=web",
    "deploy:admin": "turbo deploy --filter=admin"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  },
  "packageManager": "pnpm@10.26.2"
}
