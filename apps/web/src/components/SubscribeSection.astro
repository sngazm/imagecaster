---
import type { PodcastInfo } from "../lib/types";

interface Props {
  podcast: PodcastInfo;
}

const { podcast } = Astro.props;

// サイトの絶対URLを構築（Podcast アプリ登録用）
const siteUrl = import.meta.env.SITE_URL || Astro.site?.origin || "";
const feedPath = "/feed.xml";
const absoluteFeedUrl = siteUrl ? `${siteUrl}${feedPath}` : feedPath;

// 購読リンクがどれか設定されているか
const hasSubscribeLinks = podcast.applePodcastsUrl || podcast.spotifyUrl;
---

<section class="rounded-xl border border-zinc-200 bg-white p-6 dark:border-zinc-800 dark:bg-zinc-800">
  <h2 class="mb-4 text-lg font-bold text-zinc-900 dark:text-zinc-100">
    番組を購読
  </h2>

  <p class="mb-4 text-sm text-zinc-600 dark:text-zinc-400">
    お好みのポッドキャストアプリで購読できます
  </p>

  <div class="flex flex-wrap gap-3">
    {podcast.applePodcastsUrl && (
      <a
        href={podcast.applePodcastsUrl}
        target="_blank"
        rel="noopener noreferrer"
        class="inline-flex items-center gap-2 rounded-lg bg-gradient-to-b from-purple-500 to-purple-600 px-4 py-2 text-sm font-medium text-white shadow-sm transition-colors hover:from-purple-600 hover:to-purple-700"
      >
        <svg class="h-5 w-5" viewBox="0 0 24 24" fill="currentColor">
          <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm0 18c-4.41 0-8-3.59-8-8s3.59-8 8-8 8 3.59 8 8-3.59 8-8 8zm-1-13h2v6h-2zm0 8h2v2h-2z"/>
        </svg>
        Apple Podcasts
      </a>
    )}

    {podcast.spotifyUrl && (
      <a
        href={podcast.spotifyUrl}
        target="_blank"
        rel="noopener noreferrer"
        class="inline-flex items-center gap-2 rounded-lg bg-gradient-to-b from-green-500 to-green-600 px-4 py-2 text-sm font-medium text-white shadow-sm transition-colors hover:from-green-600 hover:to-green-700"
      >
        <svg class="h-5 w-5" viewBox="0 0 24 24" fill="currentColor">
          <path d="M12 0C5.4 0 0 5.4 0 12s5.4 12 12 12 12-5.4 12-12S18.66 0 12 0zm5.521 17.34c-.24.359-.66.48-1.021.24-2.82-1.74-6.36-2.101-10.561-1.141-.418.122-.779-.179-.899-.539-.12-.421.18-.78.54-.9 4.56-1.021 8.52-.6 11.64 1.32.42.18.479.659.301 1.02zm1.44-3.3c-.301.42-.841.6-1.262.3-3.239-1.98-8.159-2.58-11.939-1.38-.479.12-1.02-.12-1.14-.6-.12-.48.12-1.021.6-1.141C9.6 9.9 15 10.561 18.72 12.84c.361.181.54.78.241 1.2zm.12-3.36C15.24 8.4 8.82 8.16 5.16 9.301c-.6.179-1.2-.181-1.38-.721-.18-.601.18-1.2.72-1.381 4.26-1.26 11.28-1.02 15.721 1.621.539.3.719 1.02.419 1.56-.299.421-1.02.599-1.559.3z"/>
        </svg>
        Spotify
      </a>
    )}

    <a
      href={feedPath}
      target="_blank"
      rel="noopener noreferrer"
      class="inline-flex items-center gap-2 rounded-lg bg-gradient-to-b from-orange-500 to-orange-600 px-4 py-2 text-sm font-medium text-white shadow-sm transition-colors hover:from-orange-600 hover:to-orange-700"
    >
      <svg class="h-5 w-5" viewBox="0 0 24 24" fill="currentColor">
        <path d="M6.18 15.64a2.18 2.18 0 1 1 0 4.36 2.18 2.18 0 0 1 0-4.36m0-12a14.18 14.18 0 0 1 14.18 14.18h-3.27A10.91 10.91 0 0 0 6.18 6.91V3.64m0 6.55a7.64 7.64 0 0 1 7.64 7.63H10.5a4.36 4.36 0 0 0-4.32-4.36v-3.27Z"/>
      </svg>
      RSS フィード
    </a>

    <button
      type="button"
      id="copy-rss-btn"
      data-feed-url={absoluteFeedUrl}
      class="inline-flex items-center gap-2 rounded-lg border border-zinc-300 bg-white px-4 py-2 text-sm font-medium text-zinc-700 shadow-sm transition-colors hover:bg-zinc-50 dark:border-zinc-600 dark:bg-zinc-700 dark:text-zinc-300 dark:hover:bg-zinc-600"
    >
      <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect>
        <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path>
      </svg>
      <span id="copy-rss-text">URLをコピー</span>
    </button>
  </div>
</section>

<script>
  const copyBtn = document.getElementById('copy-rss-btn');
  const copyText = document.getElementById('copy-rss-text');

  copyBtn?.addEventListener('click', async () => {
    const feedUrl = copyBtn.getAttribute('data-feed-url');
    if (feedUrl) {
      await navigator.clipboard.writeText(feedUrl);
      if (copyText) {
        copyText.textContent = 'コピーしました！';
        setTimeout(() => {
          copyText.textContent = 'URLをコピー';
        }, 2000);
      }
    }
  });
</script>
