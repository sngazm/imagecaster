# 次にやること

夜のうちに進めた分の引き継ぎ。読んだら消してよい。

## すぐ効くもの（2つとも実装済み・鍵待ち）

### 1. Anthropic API キーを入れる

LLM 校正と Claude の感想が動くようになる。

```bash
cd apps/worker
npx wrangler secret put ANTHROPIC_API_KEY
```

入れたら、エピソード編集画面に次のボタンが出る。

- **感想を生成** — 文字起こしを読んだ Claude の感想を作り、公開ページに載せる
- 以後、文字起こしが完了すると自動で生成される

LLM 校正は `POST /api/episodes/:id/transcript/review` で呼べる（UI は未設置）。
辞書で拾えない誤りを直す。本文は書き換えさせず、修正提案の一覧を出させて
機械検査してから適用している。

### 2. メール通知の設定

WSL の `~/dev/imagecaster-transcriber/config.yaml` に追記する。

```yaml
notification:
  enabled: true
  smtp_host: "smtp.gmail.com"
  smtp_port: 587
  username: "自分のアドレス@gmail.com"
  password: "アプリパスワード"
  to_address: "shirahadori@gmail.com"
  episode_url_template: "https://cast.image.club/episodes/{id}/"
```

アプリパスワードは https://myaccount.google.com/apppasswords で発行する。
通常のログインパスワードでは送れない。

設定したらワーカーを再起動する。

```bash
ssh wsl
cd ~/dev/imagecaster-transcriber
pkill -f "imagecaster-transcriber worker"
setsid nohup ./scripts/run-worker.sh > /tmp/worker.log 2>&1 < /dev/null &
```

**Cloudflare Email を使わなかった理由**: `image.club` のメールは Xserver 運用で、
Cloudflare Email Sending を有効にすると MX を書き換えることになる。既存のメールに
影響するため DNS は触らなかった。

## 話者を出すには

音量の比較には音声そのものが要るので、判定は文字起こしと同時にしかできない。
既存のエピソードに話者を付ける手順:

1. エピソード編集画面で話者トラックの zip をアップロード
   （zip を選ぶと中身を読んでトラックが自動で並ぶ。BGM のトラックは名前を空にする）
2. 「音声から文字起こしをやり直す」を実行

番組全体の既定は 設定 → 文字起こし で設定済み（track1=あずま / track2=鉄塔 / track3=BGM）。

## 夜のうちにやったこと

### 取り直した6本（完了）

#280 #281 #282 #283 #284 #285 を新しい設定で文字起こしし直し、辞書を適用した。
公開サイトにも反映済み。

| ep | セグメント | 句読点を含む行 |
|---|---|---|
| #280 | 1806 → 1522 | 0 → 111 |
| #281 | 1912 → 3058 | 13 → 3034 |
| #282 | 999 → 539 | 29 → 377 |
| #283 | 2127 → 1762 | 2 → 222 |
| #284 | 762 → 794 | 3 → 785 |
| #285 | 1894 → 1621 | 0 → 96 |

取り直したあと全文検索で誤りを数えたところ、**8件検出のうち本物の誤りは1件だけ**だった。

| 検出 | 実体 | 判定 |
|---|---|---|
| プロード×2 | アップロード | 正しい |
| テッド×2 | ユナイテッドアスレ | 正しい |
| 鉄道×2 | 本物の鉄道の話 | 正しい |
| テト×1 | 山本ポテトさん | 正しい |
| **テト×1** | 「さっきテトさんが」 | **誤り** |

残った1件は辞書から意図的に外したもの（山本ポテトさんとの衝突回避）で、
LLM 校正の担当。API キーを入れれば拾える。

「鉄道さん」→「鉄塔さん」の規則が、本物の鉄道の話を壊していないことも確認できた。

**元の文字起こしは以下にバックアップしてある。**

- WSL: `~/imagecaster-transcript-backup-20260830/`
- Mac: `/private/tmp/backup_transcripts_local/`（再起動で消える）

### いちばん効いた修正

キュー経由の文字起こしで `initial_prompt` が渡っていなかった。手元のコマンドにしか
無い経路で、常時動いているワーカーは番組名も出演者名も知らないまま認識していた。

直した結果（#284・25分の回）:

| | 前 | 後 |
|---|---|---|
| 句読点を含む行 | 3 | 785 |
| 「厚刈りなんですよ」 | ✗ | 「暑がりなんですよ」 |

固有名詞の誤認識もこれが原因だった。後処理を厚くする前に入力を疑うべきだった。

### 登録した辞書（13件）

公開済み26本・約60万字を全文検索して、誤爆ゼロを確認したものだけ入れた。
順序に意味がある（「クロードコード」を「クロード」より先に置く）。

**意図的に入れなかったもの**:

- `テトさん` → ゲストの山本ポテトさんが「山本ポ鉄塔さん」になる
- `テッド` 単体 → 小説家のテッド・チャンと衝突する

取り直したあとで見つかった「さともです」「てっとです」（自己紹介の誤認識）も追加した。
最終的に残った本物の誤りは「テトさん」1件だけ。

### その他

- ハルシネーション除去（`ご視聴ありがとうございました` 11件、`ヤンヤン` 730件を実データで確認）
- 相槌の整形（「うんうんうんうんうんうん」を3回で止める）
- 同時発話の検出を冒頭30秒に限定（範囲なしだと50分で38件検出され、本物は1件だけだった）
- 編集画面の「文字起こしを表示」が生の VTT を開いていたのを、話者付きで読めるビューアに変更
- 完了済みエピソードを文字起こしからやり直す導線を追加

## 仕組みの説明

https://claude.ai/code/artifact/8c98074c-9cf3-43cb-a8dd-2d4f9659cc98

## 手を付けていないこと

- #276〜#279 は `skipTranscription: true` になっている。音声はあるので、
  やるなら編集画面で設定を外して文字起こしできる
- 公開済み全266本への一括再適用（設定 → 文字起こしの「公開済みエピソードに再適用」）は
  実行していない。辞書を確認してから判断してほしい
